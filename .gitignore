# Dependency directories
# These folders contain packages installed by your package manager (like npm or yarn).
# They can be very large and should be re-installed by anyone who clones your project.
node_modules/

# Build output
# This is where your compiled code and assets go. It's generated from your source code.
build/
dist/
.out/
.next/

# Environment variables
# These files contain sensitive data like API keys and database passwords.
# It's crucial to keep them out of your repository for security.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Log files
# These are generated by applications and servers and are not part of the source code.
logs/
*.log
npm-debug.log*
yarn-debug.log*

# Operating System generated files
# Files created by macOS, Windows, and Linux that don't belong in the repo.
.DS_Store
Thumbs.db
ehthumbs.db

# Editor and IDE configuration folders
# Your personal editor settings shouldn't be shared with other collaborators.
.vscode/
.idea/
*.suo
*.swp

# Test reports
# Results generated from running your test suites.
coverage/